{% extends 'layout.html.twig' %}

{% set breadcrumb = buildBreadcrumb([{'route': 'app_feed_index', 'trans': 'menu.collections'}], null, 'breadcrumb.add_collection') %}

{% block title %}
    {{ renderTitle(breadcrumb) }}
{% endblock %}

{% block breadcrumb %}
    {{ renderBreadcrumb(breadcrumb) }}
{% endblock %}

{% block content %}
    <div class="title-block">
        <h1>{{ 'title.add_collection'|trans }}</h1>
    </div>
    <div class="content-block">
        {{ include('App/Feed/_form.html.twig') }}
    </div>
{% endblock %}
